# 開發進度報告 - 2026/02/02

## 概要

本次開發主要完成後端程式碼整合、資料庫連接修正、單元測試建置與 CI/CD 流程設定。

---

## 完成項目

### 1. Docker 環境設定

- [x] 建立 `.env` 檔案，設定 `JWT_SECRET_KEY`
- [x] 移除 `docker-compose.yml` 中已過時的 `version` 屬性
- [x] 成功啟動三個容器：
  - `robot-db` (PostgreSQL) - port 5433
  - `robot-backend` (FastAPI) - port 8000
  - `robot-frontend` (Vite/React) - port 5173

### 2. 資料庫整合修正

| 模組 | 修正前 | 修正後 |
|------|--------|--------|
| `users.py` | 使用 `fake_users_db` 記憶體資料 | 改用 PostgreSQL `users` 表 |
| `users.py` | 訂單從 `fake_orders_db` 讀取 | 改用 PostgreSQL `orders` 表 |
| `ws.py` | 更新 `ORDER_STORE` 記憶體 | 直接更新 PostgreSQL 訂單狀態 |

### 3. 圖片路徑修正

- [x] 修正 `stores.py` 中的圖片路徑格式
- 錯誤格式：`autonomous-delivery-robot/frontend/public/images/...`
- 正確格式：`/images/stores/...`

### 4. 模型更新

- [x] `models.py` - Telemetry 的 `state` 欄位新增 `DELIVERED` 狀態
  ```python
  state: Literal["IDLE", "ASSIGNED", "MOVING", "ARRIVED", "DELIVERED"]
  ```

### 5. 程式碼清理

**main.py 清理：**
- 移除重複的 import（`Depends`, `Session`, `get_db`, `OrderDB`）
- 移除未使用的 import（`Dict`, `datetime`, `User`）
- 移除未使用的 `ORDER_STORE`, `fake_orders_db` 引用

**state.py 清理：**
- 移除 `ORDER_STORE`（改用資料庫）
- 移除 `fake_users_db`（改用資料庫）
- 移除 `fake_orders_db`（改用資料庫）
- 僅保留 `MAP_STORE` 和 `GRAPH_STORE`

### 6. 單元測試建置

建立 pytest 測試框架，共 **28 個測試案例**：

| 測試檔案 | 測試數量 | 測試範圍 |
|---------|---------|---------|
| `test_auth.py` | 6 | 註冊、登入、重複 Email、錯誤密碼 |
| `test_stores.py` | 4 | 商店列表、單一商店、商品列表 |
| `test_orders.py` | 6 | 建立訂單、取得訂單、無效地圖 |
| `test_graph.py` | 12 | Dijkstra、A*、歐幾里得距離 |

**測試設定：**
- 使用 SQLite 記憶體資料庫進行測試（不影響正式 PostgreSQL）
- 每個測試執行後自動清理資料

### 7. CI/CD 流程建置

建立 GitHub Actions workflow (`.github/workflows/ci.yml`)

**觸發條件：**
- Push 到 `main` 或 `dev` 分支
- Pull Request 到 `main` 或 `dev` 分支

**Pipeline 流程：**
```
┌─────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  backend-test   │     │  frontend-build  │     │   docker-build   │
│  (Python/pytest)│     │  (Node/ESLint)   │     │  (Docker images) │
└────────┬────────┘     └────────┬─────────┘     └────────┬─────────┘
         │                       │                        │
         └───────────────────────┴────────────────────────┘
                                 │
                          需要前兩者通過
```

### 8. 文件更新

- [x] 更新 `CLAUDE.md`，新增測試與 CI/CD 說明
- [x] 建立 `requirements-dev.txt` 開發依賴

---

## 新增/修改檔案清單

```
autonomous-delivery-robot/
├── .env                          # 新增 - JWT 金鑰設定
├── .github/
│   └── workflows/
│       └── ci.yml                # 新增 - CI/CD 設定
├── app/
│   ├── main.py                   # 修改 - 清理 import
│   ├── models.py                 # 修改 - 新增 DELIVERED 狀態
│   ├── state.py                  # 修改 - 移除未使用變數
│   ├── ws.py                     # 修改 - 改用資料庫更新
│   └── routers/
│       ├── stores.py             # 修改 - 圖片路徑
│       └── users.py              # 修改 - 改用資料庫
├── tests/                        # 新增 - 測試目錄
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_auth.py
│   ├── test_graph.py
│   ├── test_orders.py
│   └── test_stores.py
├── pytest.ini                    # 新增 - pytest 設定
├── requirements-dev.txt          # 新增 - 開發依賴
├── docker-compose.yml            # 修改 - 移除 version
└── CLAUDE.md                     # 修改 - 新增測試說明
```

---

## 測試結果

```
28 passed in 1.71s
```

所有測試通過。

---

## 待辦事項（下次開發）

- [ ] 修正 `on_event` deprecation warning（改用 FastAPI lifespan）
- [ ] 新增前端單元測試（React Testing Library）
- [ ] 設定 CORS 白名單（正式上線前）
- [ ] 移除測試用 API（`/test-db-write`, `/test-db-read`）
- [ ] 將 stores/products 資料移至資料庫

---

## 指令備忘

```bash
# 啟動 Docker 環境
docker-compose up

# 執行測試
pytest

# 執行測試並顯示覆蓋率
pytest --cov=app --cov-report=html

# 重啟後端
docker-compose restart backend

# 查看資料庫
docker exec -it robot-db psql -U robot_user -d robot_db
```
